<Window x:Class="ReringProject.UI.AccountManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReringProject.UI"
        xmlns:System="clr-namespace:System;assembly=mscorlib"
        xmlns:propTools="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
        xmlns:accountGrade="clr-namespace:ReringProject.Login"
        mc:Ignorable="d"
        Title="AccountManager" Height="400" Width="600" WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <Window.Resources>
        <ObjectDataProvider x:Key="gradeFromEnum" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="accountGrade:EAccountGrade"></x:Type>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="5"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <ListView x:Name="listView_accountList" Grid.Column="0" Grid.Row="0" FontSize="18" ItemsSource="{Binding AccountList}" SelectedIndex="{Binding SelectedIndex}" SelectedItem="{Binding SelectedAccount}" SelectionChanged="ListView_accountList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{Binding [ID], Source={StaticResource DR}}" DisplayMemberBinding="{Binding ID}" Width="150"/>
                    <GridViewColumn Header="{Binding [Type], Source={StaticResource DR}}" DisplayMemberBinding="{Binding Grade}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>

        <GridSplitter Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" ShowsPreview="True" Width="5" BorderBrush="Gray" BorderThickness="1"></GridSplitter>

        <Grid Grid.Column="2" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="7*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Column="0" Grid.Row="0" Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    
                </Grid.RowDefinitions>
                <Button Grid.Column="0" x:Name="button_new" FontSize="18" Click="Button_new_Click" Content="{Binding [New], Source={StaticResource DR}}"></Button>
                <Button Grid.Column="1" x:Name="button_delete" FontSize="18" Margin="4,0,0,0" Click="Button_delete_Click" IsEnabled="False" Content="{Binding [Delete], Source={StaticResource DR}}"></Button>
                <Button Grid.Column="2" x:Name="button_save" FontSize="18" Margin="4,0,0,0" Click="Button_save_Click" Content="{Binding [Save], Source={StaticResource DR}}"></Button>
            </Grid>

            <GroupBox x:Name="groupBox_selected" Header="Selected Account" Grid.Column="0" Grid.Row="1" >
                <StackPanel Orientation="Vertical">
                    <DockPanel Margin="4">
                        <TextBlock MinWidth="100" TextAlignment="Right" FontSize="18" Margin="0,0,4,0" Text="{Binding [ID], Source={StaticResource DR}}"></TextBlock>
                        <TextBox x:Name="textBox_id" MinWidth="100" FontSize="18"></TextBox>
                    </DockPanel>

                    <DockPanel Margin="4">
                        <TextBlock MinWidth="100" TextAlignment="Right" FontSize="18" Margin="0,0,4,0" Text="{Binding [Type], Source={StaticResource DR}}"></TextBlock>
                        <ComboBox x:Name="comboBox_grade" MinWidth="100" FontSize="18" ItemsSource="{Binding Source={StaticResource gradeFromEnum}}"></ComboBox>
                    </DockPanel>

                    <DockPanel Margin="4">
                        <TextBlock MinWidth="100" TextAlignment="Right" FontSize="18" Margin="0,0,4,0" Text="{Binding [Password], Source={StaticResource DR}}"></TextBlock>
                        <PasswordBox x:Name="textBox_password" MinWidth="100" FontSize="18"></PasswordBox>
                    </DockPanel>
                </StackPanel>
            </GroupBox>

            <Grid Grid.Column="0" Grid.Row="2" Margin="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="button_cancel" Margin="0,0,4,0" FontSize="18" Click="Button_cancel_Click" Content="{Binding [Cancel], Source={StaticResource DR}}"></Button>
                <Button Grid.Column="1" x:Name="button_ok" FontSize="18" Click="Button_ok_Click" Content="{Binding [OK], Source={StaticResource DR}}"></Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
