<Window x:Class="ReringProject.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReringProject.UI"
        mc:Ignorable="d"
        Title="Log In" 
        Height="260" 
        Width="400" 
        WindowStyle="SingleBorderWindow" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterOwner" 
        Loaded="Window_Loaded">
    <Grid Margin="20,10,20,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="5*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
        </Grid.RowDefinitions>

        <Label x:Name="label_message" Grid.Column="0" Grid.Row="0" Margin="4" Content="Log In : " FontSize="18" FontWeight="Bold" HorizontalContentAlignment="Left"></Label>
        <Label x:Name="label_logined" Grid.Column="1" Grid.Row="0" Margin="4" FontSize="18" FontWeight="Bold" HorizontalContentAlignment="Center"></Label>
        <Button x:Name="button_logOut" Grid.Column="2" Grid.Row="0" Margin="4" Content="{Binding [LogOut], Source={StaticResource DR}}" FontSize="14" Click="Button_logOut_Click"></Button>
        
        <Label Grid.Column="0" Grid.Row="1" Content="{Binding [User], Source={StaticResource DR}}" FontSize="18" HorizontalContentAlignment="Right" VerticalContentAlignment="Center"></Label>
        <ComboBox x:Name="comboBox_id" Grid.Column="1" Grid.Row="1" Margin="4" Grid.ColumnSpan="2" FontSize="20" SelectionChanged="ComboBox_id_SelectionChanged"></ComboBox>

        <Label Grid.Column="0" Grid.Row="2" Content="{Binding [Password], Source={StaticResource DR}}" FontSize="18" HorizontalContentAlignment="Right" VerticalContentAlignment="Center"></Label>
        <PasswordBox x:Name="passwordBox" Grid.Column="1" Grid.Row="2" Margin="4" FontSize="20" Grid.ColumnSpan="2" KeyUp="PasswordBox_KeyUp"></PasswordBox>

        <Grid Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="btn_edit" Margin="4" FontSize="18" Click="Btn_edit_Click" IsEnabled="False" Content="{Binding [Edit], Source={StaticResource DR}}"></Button>
            <Button Grid.Column="1" x:Name="btn_ok" Margin="4" FontSize="18" Click="Btn_ok_Click" Content="{Binding [LogIn], Source={StaticResource DR}}"></Button>
        </Grid>
    </Grid>
</Window>
