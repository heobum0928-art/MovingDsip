<UserControl x:Class="ReringProject.UI.LightGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReringProject.UI"
             xmlns:propTools="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
             mc:Ignorable="d" d:DesignWidth="400" Height="100">
    <GroupBox x:Name="groupBox" Header="{Binding Path=Name}" FontSize="18">
        <StackPanel x:Name="stackPanel">
            <CheckBox x:Name="checkBox_on" Content="{Binding [ON], Source={StaticResource DR}}" IsChecked="{Binding Path=On}"></CheckBox>
            <propTools:HeaderedEntrySlider x:Name="slider_value" Value="{Binding Path=Level}" Minimum="{Binding Min}" Maximum="{Binding Max}"></propTools:HeaderedEntrySlider>
        </StackPanel>
    </GroupBox>
</UserControl>
