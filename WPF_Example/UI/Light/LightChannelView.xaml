<UserControl x:Class="ReringProject.UI.LightChannelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReringProject.UI"
             xmlns:propTools="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="250">
    <StackPanel Orientation="Vertical" Margin="2,2,2,2">
        <CheckBox x:Name="checkBox_on" Content="{Binding Path=Name}" IsChecked="{Binding Path=On}" Margin="2,2,2,2"></CheckBox>
        <propTools:HeaderedEntrySlider x:Name="slider_level" Minimum="{Binding MinLevel}" Maximum="{Binding MaxLevel}" Value="{Binding Path=Level}" Margin="2,2,2,2">
        </propTools:HeaderedEntrySlider>

        
    </StackPanel>
</UserControl>
