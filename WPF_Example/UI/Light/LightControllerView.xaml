<UserControl x:Class="ReringProject.UI.LightControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReringProject.UI"
             xmlns:propTools="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <GroupBox x:Name="groupBox_title"  Header="{Binding Path=Name}">
        <StackPanel x:Name="stackPanel_content" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="2,2,2,2">
                <TextBlock Text="State : " Width="40"></TextBlock>
                <TextBlock x:Name="textBlock_state" Text="{Binding Path=State}" Width="40"></TextBlock>
                
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="2,2,2,2">
                <TextBlock x:Name="textBlock_port" Text="Port : " Width="50" TextAlignment="Right"></TextBlock>
                <TextBox x:Name="textBox_port" Text="{Binding Path=Port}" Width="40" TextAlignment="Center"></TextBox>
                <TextBlock x:Name="textBlock_baudrate" Text="Baudrate : " Width="60" TextAlignment="Right" Margin="4,0,0,0"></TextBlock>
                <TextBox x:Name="textBox_baudrate" Text="{Binding Path=Baudrate}" Width="60" TextAlignment="Center"></TextBox>
                <Button x:Name="button_close" Content="{Binding [Close], Source={StaticResource DR}}" Width="50" Margin="2,0,0,0" Click="Button_close_Click"></Button>
                <Button x:Name="button_open" Content="{Binding [Open], Source={StaticResource DR}}" Width="50" Margin="2,0,0,0" Click="Button_open_Click"></Button>
            </StackPanel>
            
        </StackPanel>
    </GroupBox>
</UserControl>
