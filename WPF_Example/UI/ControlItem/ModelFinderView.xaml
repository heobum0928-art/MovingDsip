<UserControl x:Class="ReringProject.UI.ModelFinderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReringProject.UI"
             xmlns:propTools="clr-namespace:PropertyTools.Wpf;assembly=PropertyTools.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="300">
    <UserControl.Resources>
        <local:RectConverter x:Key="RectToStringConverter"></local:RectConverter>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox x:Name="textBox_rect" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" Margin="2" FontSize="14" Text="{Binding Master, Converter={StaticResource RectToStringConverter}}"></TextBox>
        <Button x:Name="button_new" Grid.Column="0" Grid.Row="2" Margin="2" FontSize="14">New..</Button>
        <Button x:Name="button_edit" Grid.Column="1" Grid.Row="2" Margin="2" FontSize="14">Edit..</Button>
        <Button x:Name="button_load" Grid.Column="2" Grid.Row="2" Margin="2" FontSize="14">Load..</Button>

    </Grid>
</UserControl>
