<Window x:Class="ReringProject.UI.ProcessMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReringProject.UI"
        mc:Ignorable="d"
        Title="Process Monitor" Height="600" Width="600" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dataGrid_sequence" Grid.Column="0" Grid.Row="0" VerticalScrollBarVisibility="Auto" ItemsSource="{Binding Path=StateList}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Path=Type, Mode=OneWay}" Width="100"></DataGridTextColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name, Mode=OneWay}" Width="120"></DataGridTextColumn>
                <DataGridTextColumn Header="State" Binding="{Binding Path=State, Mode=OneWay}" Width="100"></DataGridTextColumn>
                <DataGridTextColumn Header="Action" Binding="{Binding Path=Action, Mode=OneWay}" Width="100"></DataGridTextColumn>
                <DataGridTextColumn Header="Time" Binding="{Binding Path=Time, Mode=OneWay}" Width="80"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Vertical">
            <Button x:Name="button_forceExit" Content="Force Idle"></Button>
        </StackPanel>
    </Grid>
</Window>
