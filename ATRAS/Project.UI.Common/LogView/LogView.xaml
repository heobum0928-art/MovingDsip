<UserControl x:Class="Project.UI.Common.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Project.UI.Common"
             d:DesignHeight="600" d:DesignWidth="1000" 
             mc:Ignorable="d" GotFocus="UserControl_GotFocus">
    
    <Grid>
        <local:ListViewEx Background="#2c2c2c" ItemsSource="{Binding LogItems}" HorizontalAlignment="Stretch"
                            MouseMove="OnMouseMove"    
                            x:Name="listViewEx"
                          SelectedIndex="{Binding SelectedIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            SelectionMode="Extended">
            <local:ListViewEx.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="LogClear" Click="MenuItem_Click"/>
                </ContextMenu>
            </local:ListViewEx.ContextMenu>
            <local:ListViewEx.Resources>
                <Style TargetType="local:ListViewExItem">
                    <Style.Triggers>
                       <DataTrigger Binding="{Binding Path=LogEvent.Level}" Value="DEBUG">
                            <Setter Property="Foreground" Value="White" />
                        </DataTrigger>
                      <DataTrigger Binding="{Binding Path=LogEvent.Level}" Value="ERROR">
                        <Setter Property="Foreground" Value="OrangeRed" />
                      </DataTrigger>
                            <DataTrigger Binding="{Binding Path=LogEvent.Level}" Value="WARN">
                            <Setter Property="Foreground" Value="Orange" />
                        </DataTrigger>
                            <DataTrigger Binding="{Binding Path=LogEvent.Level}" Value="INFO">
                            <Setter Property="Foreground" Value="LightGreen" />
                        </DataTrigger>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value ="Black" />
                        <Setter Property="Background" Value ="Black" />
                        <Setter Property="Foreground" Value ="White" />
                      </Trigger>
                      <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value ="Black" />
                        <Setter Property="BorderBrush" Value ="Black" />
                        <Setter Property="Foreground" Value ="White" />
                      </Trigger>
                      
                    </Style.Triggers>
                  <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                  <!--<Setter Property="IsSelected" Value="{Binding IsSelected,Mode=OneWayToSource}"/>-->
                </Style>
            </local:ListViewEx.Resources>
            <local:ListViewEx.View>
                <GridView>
                    <GridViewColumn Header="Time" DisplayMemberBinding="{Binding LogEvent.TimeStamp}"  />
                    <GridViewColumn Header="Thread" DisplayMemberBinding="{Binding LogEvent.ThreadName}" />
                    <GridViewColumn Header="Logger" DisplayMemberBinding="{Binding LogEvent.LoggerName}" Width="120" />
                    <GridViewColumn Header="Level" DisplayMemberBinding="{Binding LogEvent.Level}"/>
                    <GridViewColumn Header="Message" DisplayMemberBinding="{Binding LogEvent.Message}" Width="500"/>                    
                    <GridViewColumn Header="Method" DisplayMemberBinding="{Binding LogEvent.MemberName}"  Width="160"/>
                    <GridViewColumn Header="File" DisplayMemberBinding="{Binding LogEvent.FileName}"  Width="160"/>
                    <GridViewColumn Header="Line" DisplayMemberBinding="{Binding LogEvent.LineNumber}"  Width="50"/>
                    <GridViewColumn Header="Exception" DisplayMemberBinding="{Binding LogEvent.Exception}"  Width="300"/>
                </GridView>
            </local:ListViewEx.View>
        </local:ListViewEx>
    </Grid>
</UserControl>
