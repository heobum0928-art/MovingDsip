<UserControl x:Class="Project.UI.Common.DBEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"              
             xmlns:local="clr-namespace:Project.UI.Common"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d" 
             TextOptions.TextFormattingMode="Display"
             d:DesignHeight="500" d:DesignWidth="700">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Project.UI.Common;component/Resources/Images.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <ToolBar.Resources>
                <Style TargetType="{x:Type Image}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type ButtonBase}, AncestorLevel=1}, Path=IsEnabled}" Value="False">
                            <Setter Property="Opacity" Value="0.30" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ToolBar.Resources>

            <Button Click="openFileClick">
                <Image Source="{StaticResource Open}" Height="16"/>
            </Button>
            <Button Click="saveFileClick">
                <Image Source="{StaticResource Save}" Height="16"/>
            </Button>
            <Separator/>
            <Button Command="Cut">
                <Image Source="{StaticResource Cut}" Height="16"/>
            </Button>
            <Button Command="Copy">
                <Image Source="{StaticResource Copy}" Height="16"/>
            </Button>
            <Button Command="Paste">
                <Image Source="{StaticResource Paste}" Height="16"/>
            </Button>
            <Button Command="Delete">
                <Image Source="{StaticResource Delete}" Height="16"/>
            </Button>
            <Separator/>
            <Button Command="Undo">
                <Image Source="{StaticResource Undo}" Height="16"/>
            </Button>
            <Button Command="Redo">
                <Image Source="{StaticResource Redo}" Height="16"/>
            </Button>
            <Separator/>

        </ToolBar>
        <avalonEdit:TextEditor            
            Name="textEditor"
            FontFamily="Consolas"
            FontSize="10pt"
            SyntaxHighlighting="XML"
            ShowLineNumbers="True"
            TextChanged="textEditor_TextChanged">
            Empty!
        </avalonEdit:TextEditor>
       
    </DockPanel>


</UserControl>
