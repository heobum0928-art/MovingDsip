<UserControl x:Class="Project.DeepLearning.UI.MultiSampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.DeepLearning.UI"
             mc:Ignorable="d" 
             
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded"
             
             d:DesignHeight="600" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" 
                Command="{Binding MultiSampleLoadCommand}"
                Margin="10, 4"
                FontWeight="Bold" FontSize="15"
                Content="Multi Sample Load"/>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Label Margin="10, 4" FontWeight="Bold" FontSize="25" Content="Multi Predict Result"/>
            <Label Margin="10, 4" FontWeight="Bold" FontSize="25" Content="{Binding MultiPredictResult}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
